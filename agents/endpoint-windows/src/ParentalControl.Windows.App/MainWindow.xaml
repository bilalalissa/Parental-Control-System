<Window x:Class="ParentalControl.Windows.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Parental Control Child" Height="720" Width="940"
        MinHeight="620" MinWidth="760" WindowStartupLocation="CenterScreen"
        Background="#F5F7FA" Icon="assets/ParentalControlChild.ico">
  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="32">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <Grid Grid.Row="0">
        <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
          <Image Source="assets/ParentalControlChild.ico" Width="72" Height="72" Margin="0,0,20,0" />
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="VISIBLE FAMILY ENDPOINT" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" />
            <TextBlock Text="Parental controls are visible and active" FontSize="26" FontWeight="SemiBold" Margin="0,5,0,0" />
            <TextBlock Text="This Windows PC communicates directly with your parent controller on the local network." Foreground="#626B76" Margin="0,5,0,0" />
          </StackPanel>
        </StackPanel>
        <Button Grid.Column="1" Content="Refresh" Padding="18,8" VerticalAlignment="Top" Click="Refresh_Click" />
      </Grid>

      <Border Grid.Row="1" Margin="0,28,0,0" Padding="22" CornerRadius="12" Background="White" BorderBrush="#D9DEE5" BorderThickness="1">
        <Grid>
          <Grid.ColumnDefinitions><ColumnDefinition Width="220" /><ColumnDefinition /></Grid.ColumnDefinitions>
          <StackPanel>
            <TextBlock Text="Service" /><TextBlock Text="Controller" Margin="0,12,0,0" />
            <TextBlock Text="Device" Margin="0,12,0,0" /><TextBlock Text="System" Margin="0,12,0,0" />
            <TextBlock Text="Architecture" Margin="0,12,0,0" /><TextBlock Text="Session" Margin="0,12,0,0" />
            <TextBlock Text="Uptime" Margin="0,12,0,0" />
          </StackPanel>
          <StackPanel Grid.Column="1">
            <TextBlock x:Name="ServiceValue" FontWeight="SemiBold" /><TextBlock x:Name="ControllerValue" Margin="0,12,0,0" />
            <TextBlock x:Name="DeviceValue" Margin="0,12,0,0" /><TextBlock x:Name="SystemValue" Margin="0,12,0,0" TextWrapping="Wrap" />
            <TextBlock x:Name="ArchitectureValue" Margin="0,12,0,0" /><TextBlock x:Name="SessionValue" Margin="0,12,0,0" />
            <TextBlock x:Name="UptimeValue" Margin="0,12,0,0" />
          </StackPanel>
        </Grid>
      </Border>

      <Border Grid.Row="2" Margin="0,18,0,0" Padding="22" CornerRadius="12" Background="White" BorderBrush="#D9DEE5" BorderThickness="1">
        <StackPanel>
          <TextBlock Text="Adult pairing" FontSize="20" FontWeight="SemiBold" />
          <TextBlock Text="Paste the one-time invitation from the Parent Controller. Windows administrator authorization is required; there is no child-accessible override." Foreground="#626B76" TextWrapping="Wrap" Margin="0,8,0,12" />
          <TextBox x:Name="InvitationText" MinHeight="60" TextWrapping="Wrap" AcceptsReturn="True" />
          <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Pair this PC" Padding="18,8" Click="Pair_Click" />
            <TextBlock x:Name="PairingResult" Margin="14,8,0,0" Foreground="{StaticResource AccentBrush}" TextWrapping="Wrap" />
          </StackPanel>
        </StackPanel>
      </Border>

      <Border Grid.Row="3" Margin="0,18,0,0" Padding="22" CornerRadius="12" Background="White" BorderBrush="#D9DEE5" BorderThickness="1">
        <StackPanel>
          <TextBlock Text="Privacy and scope" FontSize="20" FontWeight="SemiBold" />
          <TextBlock x:Name="PrivacyValue" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="#4E5864" />
          <TextBlock Text="Stage 07 is status-only. Application monitoring, browser data, chat, schedules, locking, and other enforcement are not present in this Windows release." Margin="0,10,0,0" TextWrapping="Wrap" Foreground="#8C332C" FontWeight="SemiBold" />
          <TextBlock x:Name="NetworkValue" Margin="0,14,0,0" TextWrapping="Wrap" FontFamily="Consolas" />
        </StackPanel>
      </Border>
    </Grid>
  </ScrollViewer>
</Window>
