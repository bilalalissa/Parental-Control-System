<Window x:Class="ParentalControl.Windows.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Parental Control Child" Height="780" Width="980"
        MinHeight="660" MinWidth="800" WindowStartupLocation="CenterScreen"
        Background="#F5F7FA" Icon="assets/ParentalControlChild.ico">
  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="32">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <Grid Grid.Row="0">
        <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
          <Image Source="assets/ParentalControlChild.ico" Width="72" Height="72" Margin="0,0,20,0" />
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="VISIBLE FAMILY ENDPOINT" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" />
            <TextBlock Text="Parental controls are visible and active" FontSize="26" FontWeight="SemiBold" Margin="0,5,0,0" />
            <TextBlock Text="This Windows PC communicates directly with your parent controller on the local network." Foreground="#626B76" Margin="0,5,0,0" />
          </StackPanel>
        </StackPanel>
        <Button Grid.Column="1" Content="Refresh" Padding="18,8" VerticalAlignment="Top" Click="Refresh_Click" />
      </Grid>

      <TabControl Grid.Row="1" Margin="0,24,0,0" MinHeight="560">
        <TabItem Header="Status">
          <StackPanel Margin="18">
            <Border Padding="22" CornerRadius="12" Background="White" BorderBrush="#D9DEE5" BorderThickness="1">
              <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="220" /><ColumnDefinition /></Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock Text="Service" /><TextBlock Text="Controller" Margin="0,12,0,0" />
                  <TextBlock Text="Device" Margin="0,12,0,0" /><TextBlock Text="System" Margin="0,12,0,0" />
                  <TextBlock Text="Architecture" Margin="0,12,0,0" /><TextBlock Text="Session" Margin="0,12,0,0" />
                  <TextBlock Text="Uptime" Margin="0,12,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="1">
                  <TextBlock x:Name="ServiceValue" FontWeight="SemiBold" /><TextBlock x:Name="ControllerValue" Margin="0,12,0,0" />
                  <TextBlock x:Name="DeviceValue" Margin="0,12,0,0" /><TextBlock x:Name="SystemValue" Margin="0,12,0,0" TextWrapping="Wrap" />
                  <TextBlock x:Name="ArchitectureValue" Margin="0,12,0,0" /><TextBlock x:Name="SessionValue" Margin="0,12,0,0" />
                  <TextBlock x:Name="UptimeValue" Margin="0,12,0,0" />
                </StackPanel>
              </Grid>
            </Border>
            <Border Margin="0,18,0,0" Padding="22" CornerRadius="12" Background="White" BorderBrush="#D9DEE5" BorderThickness="1">
              <StackPanel>
                <TextBlock Text="Adult pairing" FontSize="20" FontWeight="SemiBold" />
                <TextBlock Text="Paste the one-time invitation from the Parent Controller. Windows administrator authorization is required; there is no child-accessible override." Foreground="#626B76" TextWrapping="Wrap" Margin="0,8,0,12" />
                <TextBox x:Name="InvitationText" MinHeight="60" TextWrapping="Wrap" AcceptsReturn="True" />
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                  <Button Content="Pair this PC" Padding="18,8" Click="Pair_Click" />
                  <TextBlock x:Name="PairingResult" Margin="14,8,0,0" Foreground="{StaticResource AccentBrush}" TextWrapping="Wrap" />
                </StackPanel>
              </StackPanel>
            </Border>
          </StackPanel>
        </TabItem>

        <TabItem Header="Activity">
          <StackPanel Margin="18">
            <TextBlock Text="Application activity" FontSize="20" FontWeight="SemiBold" />
            <TextBlock x:Name="ActivitySummary" Margin="0,6,0,10" Foreground="#626B76" TextWrapping="Wrap" />
            <ListBox x:Name="ApplicationList" MinHeight="150" MaxHeight="220" />
            <TextBlock Text="Browser sharing" FontSize="20" FontWeight="SemiBold" Margin="0,20,0,0" />
            <TextBlock x:Name="BrowserSummary" Margin="0,6,0,10" Foreground="#626B76" TextWrapping="Wrap" />
            <ListBox x:Name="BrowserList" MinHeight="120" MaxHeight="200" />
            <TextBlock Text="Only application names/executable identities and enrolled tab titles/query-free origins are eligible. Window contents, paths, command lines, private tabs, page contents, forms, cookies, and traffic are excluded." Margin="0,12,0,0" TextWrapping="Wrap" Foreground="#4E5864" />
          </StackPanel>
        </TabItem>

        <TabItem Header="Chat">
          <StackPanel Margin="18">
            <TextBlock Text="Family chat" FontSize="20" FontWeight="SemiBold" />
            <TextBlock Text="Text only · bounded protected local retention · no attachments or automatic links" Margin="0,6,0,10" Foreground="#626B76" />
            <ListBox x:Name="ChatList" MinHeight="280" MaxHeight="360" />
            <TextBox x:Name="ChatDraft" Margin="0,12,0,0" MinHeight="58" MaxLength="2000" AcceptsReturn="True" TextWrapping="Wrap" />
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
              <Button Content="Send to parent" Padding="18,8" Click="SendChat_Click" />
              <Button Content="Mark parent messages read" Padding="18,8" Margin="10,0,0,0" Click="MarkRead_Click" />
              <TextBlock x:Name="ChatResult" Margin="14,8,0,0" Foreground="{StaticResource AccentBrush}" TextWrapping="Wrap" />
            </StackPanel>
          </StackPanel>
        </TabItem>

        <TabItem Header="Request Time">
          <StackPanel Margin="18">
            <TextBlock Text="Request more time" FontSize="20" FontWeight="SemiBold" />
            <TextBlock Text="Ask your parent for 5–240 minutes. A short optional note is sent only with this request." Margin="0,6,0,14" Foreground="#626B76" TextWrapping="Wrap" />
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="Minutes" VerticalAlignment="Center" />
              <TextBox x:Name="MinutesText" Text="15" Width="70" Margin="12,0,0,0" />
            </StackPanel>
            <TextBox x:Name="TimeNote" Margin="0,12,0,0" MinHeight="70" MaxLength="500" AcceptsReturn="True" TextWrapping="Wrap" />
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
              <Button Content="Send request" Padding="18,8" Click="RequestTime_Click" />
              <TextBlock x:Name="TimeRequestResult" Margin="14,8,0,0" Foreground="{StaticResource AccentBrush}" TextWrapping="Wrap" />
            </StackPanel>
          </StackPanel>
        </TabItem>

        <TabItem Header="Privacy">
          <StackPanel Margin="18">
            <TextBlock Text="Privacy and scope" FontSize="20" FontWeight="SemiBold" />
            <TextBlock x:Name="PrivacyValue" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="#4E5864" />
            <TextBlock Text="Stage 08 does not add schedules, locking, logoff, restart, shutdown, website restrictions, or other Windows enforcement. Parent-controlled collection switches are read-only here. An authorized local administrator can remove or bypass this software." Margin="0,12,0,0" TextWrapping="Wrap" Foreground="#8C332C" FontWeight="SemiBold" />
            <TextBlock x:Name="NetworkValue" Margin="0,18,0,0" TextWrapping="Wrap" FontFamily="Consolas" />
          </StackPanel>
        </TabItem>
      </TabControl>
    </Grid>
  </ScrollViewer>
</Window>
